import { SearchHistoryPipe } from './search-history.pipe';

describe('SearchHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
